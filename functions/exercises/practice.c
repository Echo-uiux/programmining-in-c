# include <stdio.h>



double triangle_area( double base, double height)

// take input for triangle base and height in a main and print
double main (void)
{
    printf("enter digits/");
    scanf("%d %d", = (triangle_area(double, double)) );

    return 0;
}
// function for computation of the area
{
    double product;
    product = base * height;
    return product / 2;
}